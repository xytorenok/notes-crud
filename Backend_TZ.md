
# ТЗ: Backend-разработчик (Модуль NoteKeeper API)

## 1. Задача
Разработать REST API сервер на Node.js с интеграцией MongoDB.

## 2. Структура проекта
Исполнитель предоставляет файлы в корне репозитория:
- `server.js` — основной файл сервера.
- `package.json` — зависимости (только `mongodb`).
- `.env.example` — шаблон переменных окружения.

## 3. Технические требования
- **База данных**: MongoDB Atlas. Использовать БД с именем `notes_app`.
- **Сервер**: Нативный модуль `http` или библиотека `express` (на выбор, но без перегрузки лишним весом).
- **Статика**: Сервер **обязан** отдавать статические файлы (html, js, css) из папки `./public`.
- **Конфигурация**: Порт и URI базы данных должны браться из `process.env`.

## 4. Спецификация API
- Префикс всех роутов: `/api`.
- Формат обмена данными: JSON.
- Кодировка: UTF-8.
- Сортировка заметок: От новых к старым (`_id: -1`).

## 5. Ожидаемая структура БД
- Коллекция `users`: `{ _id, email, password }`.
- Коллекция `notes`: `{ _id, userId, content, date }`.
